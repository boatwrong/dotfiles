import numpy as np

tInf = 25 # Celsius
t0 = 100 # Celsius
d = 0.01 # diameter (m)
L = 0.1 # height (m)
k = 401 # W/mK
h - 15 # W/m^2 K
P = np.pi * d # Perimiter (m)

M = h * P / (k * Ac)
m = pow(m, 0.5)

numerator = np.cosh(L - x) + (h / (m * k)) * np.sinh(m * (L - x))
denominator = np.cosh(m * L) + (h / (m * k) * np.sinh(m * L))

